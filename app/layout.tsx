import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "VFX Director Pipeline | Professional Scene Breakdown Tool",
  description: "AI-powered VFX pipeline supervisor for cinematic scene breakdowns, particle simulation, lighting, and rendering guidance",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
